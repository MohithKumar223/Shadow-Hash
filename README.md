# Shadow-Hash
This repository contains a simple Flask backend for customer health scoring.

## Day 1 Progress
- Backend API created using Flask
- Customer endpoint implemented
- Health score logic added

## Day 2 Progress
- Added `backend/data.py` to store sample customer data
- Added `backend/utils.py` to calculate health status
- Added `backend/route.py` to define API routes and return customer data with health scores
- Added `.gitignore` to ignore virtual environment and cache files
- Cleaned up project structure and committed the Day 2 updates